# C (Static Library) Make Project Template

For those who have memory of a goldfish like me.

## Notes
- If using `clangd`, create `compile_commands.json` using [`bear`](https://github.com/rizsotto/Bear).
- Why not just use CMake?